<Window x:Class="AbfWatcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AbfWatcher"
        mc:Ignorable="d"
        Title="Abf Watcher" Height="300" Width="600">
    <DockPanel>

        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
            <Label Content="ABFs Tracked:" />
            <Label Content="{Binding FilesTracked}" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
            <Label Content="Watch Folder:" />
            <Label Content="{Binding WatchFolder}" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
            <Label Content="Database File:" />
            <Label Content="{Binding DatabaseFile}" />
        </StackPanel>

        <Button Content="Manual Create" DockPanel.Dock="Bottom" Margin="5" Click="Button_Click" />

        <ScrollViewer Background="LightGray" Padding="5" FontFamily="consolas"
            HorizontalScrollBarVisibility="Visible"
            VerticalScrollBarVisibility="Visible">
            <TextBlock Text="{Binding LogText}"/>
        </ScrollViewer>
    </DockPanel>
</Window>
